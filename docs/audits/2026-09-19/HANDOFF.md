# 审查后的交接待办

被审查提交：edb97fd1936b4c9ddc01d1a09773a63de5ba1646。
原始归档提交为 `61b00bc`，仅记录问题。用户随后授权修复；以下勾选表示本轮已实现并通过对应自动化回归，**不表示真机验收完成或允许合并**。实现与测试边界见 [FIXES.md](FIXES.md)，原始反例见 [REVIEW.md](REVIEW.md)。

## 架构/规范轴

- [x] A1：普通表达式 OK/ENTER 与 EXE 统一后台计算；表单下一格仍同步。
- [x] A2：数值循环响应取消，共享时间/工作量预算；取消后新任务可执行。
- [x] A3：粘贴、触摸光标/选区、格选择统一失效旧任务。
- [x] A4：显示区与数字键按 pointerId 独立释放；失焦/销毁清理重复任务。

## 功能/结果轴

- [x] B1：Base-N 按当前进制校验，HEX 1E 可转换为 DEC 30。
- [x] B2：矩阵/向量按有效位显示，小量不舍成 0，非有限值拒绝。
- [x] B3：工作流错误退出重新加载选中单格，内部求值串不再污染频数。
- [x] B4：重复 HOME 保留来源应用，跨应用清理验证/临时矩阵向量答案。
- [x] B5：无实根不等式正确取样，区间端点和小虚根分类有回归。
- [x] B6：度数不超过四的精确重因子分离；迭代有收敛/残差/取消检查，不收敛明确报错。仍不承诺任意病态近重根都能求出。
- [x] B7：复数 log 使用底数 10，与普通路径一致。
- [x] B8：零的正幂返回 0；未定义零幂/零对数及非有限结果拒绝。
- [x] B9：方差及线性/变换线性回归中心化，覆盖大偏移和频数；二次回归的更广泛稳定性另列后续。
- [x] B10：FORMAT、ENG 和恢复显示作用于当前结果 payload，不覆盖最新 Ans。
- [x] B11：复数验证进入正式源码并保留 Ans；仅计算/复数应用开放入口。

## 数值附录后续项

- [x] 已复现的极小系数/根绝对截断、复平方根小分量、非零小虚部显示及负大数 asinh。
- [x] 精确整数象限使用精确轴值；DEG/RAD/GRAD 和近轴不吸附有回归，sin(180) 的后续除法正确报错。
- [ ] 更极端的系数缩放、近重根和二次回归数值条件数验证。
- [x] P2：普通实根极小虚部残差已在 2026-09-20 用求根层有理区间确认修复，并保留真实小虚部回归及 MuMu 的 1/2/3 截图；原始 PolynomialDisplayProbe 与输出仍留档。方法和边界见[第二轮记录](../2026-09-20/README.md)。
- [x] 标量/复数隐式乘法优先级一致性：第三轮沿用现有标量规则，补公开按键/粘贴、关系验证和 192 组生成表达式检查；见[规则、反例和模拟器截图](../2026-09-20-grammar/README.md)。
- [ ] 一般超越函数等式的误差与精确值政策，不可用全局小量清零掩盖误差。

## 继续方式

本轮未重写架构，而是在现有边界修补，并把反例接入永久回归。下一步先验收本轮修改，不继续盲目加功能。

- [x] 首轮修复 `fce5c2b` 云端核心/Android/输入主机回归通过（Actions run 35424683756）；第二轮以其自身 CI 为准。
- [ ] 使用长期签名测试包做本轮定向设备检查：OK/EXE/AC、慢计算期间粘贴、多指交叉抬起、退后台和重进。
- [ ] 真机同亮度同环境下对比空闲、普通计算、受限大计算、退后台的耗电与温度；模拟器不能代替这些结论。用户于 2026-09-20 明确要求此项延后，先处理其他功能/架构问题。
- [ ] 抽查本轮修复的计算反例及历史 FORMAT，不把主机测试当成实机已验收。

此前 artifact 10495340801 已通过的函数表分页、结果页隔离和覆盖安装无需无故重复；它并未验收后来新增的矩阵记忆、频数统计或 Base-N 表单。

README、ARCHITECTURE、Stage 6 relay 已加入当前修复和资源边界；FEATURE_COVERAGE 已明确标记旧矩阵为历史。旧 patch 脚本不再作为执行入口，正式源码和永久回归才是本轮实现。

PR #9 继续保持 Draft；所有修复和集中验收完成后，仍须用户明确授权才能合并。
