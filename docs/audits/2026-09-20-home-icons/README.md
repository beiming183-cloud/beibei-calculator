# 方程与不等式图标微调

按用户最终确认：方程使用 `x = y`，不等式使用 `x < y`。取消中途曲线/数轴设计，使用正常字重、18sp、水平居中的完整表达式，保留原配色、卡片与标签位置。

仅改主屏两项绘制，不改计算逻辑、交互或后台任务。27 项界面检查增加完整符号断言；构建和 lint 日志见本目录。MuMu 独立 debug 包截图见 [home.png](home.png)，正式包未动。
