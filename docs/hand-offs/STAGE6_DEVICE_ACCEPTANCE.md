# Stage 6 集中真机验收清单

更新日期：2026-09-17

分支：`stage6/integration`

PR：#9（保持 Draft，真机验收完成且用户明确授权前不合并）

当前复测包：Actions run `35215973055` 生成的长期签名 artifact `stage6-table-result-fix-signed-apk`，artifact ID `10495340801`。函数表 TABLE 状态隔离修复已完成云端门禁与 MuMu 复验；该包也已通过 `adb install -r` 对现有 `com.beibei.calculator` 的同包覆盖安装验证。

此前集中验收基线：Actions run `35197523499` 生成的 `stage6-audit-signed-apk`，artifact ID `10486064308`。

已通过云端门禁：完整 `:core:check`、Cn991 Debug、Cn991 Release + R8、长期证书指纹、`com.beibei.calculator` 包名硬校验。

永久自动化：`CnCwStage6AcceptanceSuite` 已挂入 `:core:check`。run `35220348788` 从 HOME 和真实按键入口覆盖不等式 CHOICE、DEL、空白/非法输入定位、结果返回输入，两种比例结构化求值与旧逗号桥，以及双函数 workflow 的结果协议和 BACK 数据保留。随后 commit `5bb38c502dd51988155065c4e97746052f1e42c6` 继续加入统计/矩阵/向量 WorkflowAction 边界回归，run `35226498841` 已通过完整核心回归、Android Debug 构建和 artifact 保存。因此这些项目后续人工主要检查 Android 标签、布局、disabled 视觉和触控命中，不再重复证明 core 逻辑。

## 0. 安装与升级链

- [x] 不卸载当前北北计算器，直接安装长期签名测试 APK；artifact `10495340801` 使用 `adb install -r` 覆盖安装成功，未出现签名冲突或第二个重复应用。
- [x] 首次安装时间保持不变，确认属于同一个 `com.beibei.calculator` 的覆盖升级。
- [ ] 安装后能正常启动，默认进入空白“计算”工作区，不停留在 HOME 菜单。
- [ ] HOME、返回、设置、ON/OFF 基本导航正常。
- [ ] 此轮只做测试，不创建正式 GitHub Release，不合并 PR #9。

## 1. 0.3.17 / 0.3.18 延后项集中复查

- [ ] 普通计算 `2+3×4` 得到 `14`，再输入 `÷2` 可从 Ans 继续得到 `7`。
- [ ] 分数、根式、幂的自然显示没有退回 `^` / 纯文本拼接。
- [ ] 长数字与 SCI 结果不显示裸 `E`，应保持自然 `×10` 上标形式。
- [ ] 长按结果可以进入复制菜单；“复制计算过程 / 结果 / Ans”均可正常使用。
- [ ] 长按公式、拖动选择手柄、复制选区、粘贴均可用。
- [ ] 光标左右移动、分数上下槽位、幂指数槽位的语义移动没有退化。
- [ ] DEL、AC、历史 ↑↓、Page↑↓、Shift 一次性状态均无明显回归。
- [ ] ON / HOME / 设置 / 返回以及方向区触摸目标看起来和按起来都不再偏小。

## 2. 函数表：结构化输入 + TABLE 结果

### 单函数

- [x] 修复复验样例：`f(x)=x`，开始 `0`，结束 `20`，步长 `1` 正常生成 21 行表格。
- [x] 进入 TABLE 结果页后，顶部不再显示内部编辑串 `x,0,20,1` 或编辑光标。
- [x] ↓：`1–4/21` → `2–5/21`。
- [x] PageDown：到 `6–9/21`。
- [x] PageUp：回到 `2–5/21`。
- [x] `x / f(x)` 两列无错位，分页无回归。

### 双函数

- [x] core 自动化：进入 `f(x), g(x)` 会得到五字段 workflow；`x, x+10, 1, 6, 1` 可生成三列 TABLE；BACK 后 `g(x)=x+10` 等输入仍保留。
- [ ] Android 人工只需看：字段标签、三列表格视觉对齐和触控/分页表现。

> 多项式调阶与联立方程增减元数已在此前集中验收通过，用户明确无需再次测试；函数表单函数修复复验也已通过，不再重复这些项目。

## 3. 不等式：二 / 三 / 四次闭环

### 已由 CI 自动守住

- [x] 二/三/四次分别发布关系 + 3/4/5 个系数。
- [x] 关系格是 CHOICE；普通数字不能覆盖关系值。
- [x] 方向键循环 `> / < / ≥ / ≤`，DEL 恢复默认 `>`。
- [x] 空白时 EXE 定位第一系数；非法表达式停在错误格并显示输入格式错误。
- [x] 完整二次不等式可计算；BACK 后原系数保留。

### 仍需 Android 人工确认

- [ ] 关系符号显示与焦点高亮直观，没有被普通文本编辑光标覆盖。
- [ ] 方向键切换关系时触控/视觉反馈正常。
- [ ] 三次、四次输入页列宽/标签不挤压。
- [ ] 建议各看一组：`x²-1>0`、`x³-x>0`、`x⁴-1≤0`，重点看结果显示，不再重复验证底层求解算法。

## 4. 比例：标签输入 + 旧逗号兼容

### 已由 CI 自动守住

- [x] `A:B=X:D` 标签为 `A / B / D`，`2,4,10` 得 `X=5`。
- [x] `A:B=C:X` 第三标签为 `C`，`2,4,3` 得 `X=6`。
- [x] 结果使用结构化 `KEY_VALUE`。
- [x] BACK 后字段数据保留。
- [x] 首字段直接输入逗号会退出 structured workflow 回到旧逗号桥，并能继续正确求解。

### 仍需 Android 人工确认

- [ ] 两种比例页标签、焦点、结果排版正确。
- [ ] 首字段逗号回退时界面切换自然，不出现残留表格/双光标。

## 5. WorkflowAction 操作条

### 统计

- [x] core 自动化：一元统计固定发布“新增数据 / 删除当前行 / 计算 / 返回”4 个动作；最小行数时“删除当前行” disabled；输入完整后“计算”启用；增行后可删除并且不能删到最小行数以下。
- [x] 操作条基础触控已在 MuMu 验收：四按钮等宽、间距明确，“提高阶数 / 降低阶数”命中正常，未见相邻误触。
- [ ] Android 人工：统计页 disabled 视觉清楚，点击 disabled 不出现错误跳转。
- [ ] 新增 3 个数据 `1,2,3` 后计算，均值应为 `2`。

### 矩阵 / 向量

- [x] core 自动化：矩阵固定 6 动作顺序为“减少行 / 增加行 / 减少列 / 增加列 / 计算 / 返回”；`1×1` 最小边界和 `4×4` 最大边界均有 disabled，越界动作被拒绝，缩小后相应增大动作重新启用。
- [x] core 自动化：向量固定 6 动作；`1×2` 起始边界与 `2×3` 最大边界均有 disabled，向量数/维度不能越界，回到最小时减少动作重新 disabled。
- [ ] Android 人工只需看：矩阵“行/列”、向量“向量数/维度”的标签是否直观；6 个按钮是否同排、均能准确命中且没有点 A 触发 B；disabled 状态视觉是否明显。

## 6. 已通过：调阶 / 调元数时数据语义保持

### 多项式

- [x] 二次 → 三次时只新增空白最高阶 `a3`，原 `a2/a1/a0` 保持原语义位置。
- [x] 三次 → 二次时只丢弃最高阶，原低阶系数保持。
- [x] 该项无需再次测试。

### 联立方程

- [x] 二元 → 三元时原变量系数保持，新变量列为空，RHS `b` 始终位于最右侧。
- [x] 三元 → 二元时原系数与 RHS 恢复正确。
- [x] 该项无需再次测试。

## 7. 错误、结果与历史状态

- [x] core 自动化：结构化不等式空白 EXE 定位第一空白格；非法表达式定位错误格，不直接进入 Math ERROR 结果页。
- [x] core 自动化：成功不等式/比例/双函数结果 → BACK → 输入，workflow 数据保留。
- [ ] Android 人工：错误格高亮/提示位置直观，修正后可以继续，不需要重开应用。
- [ ] Android 人工：错误 → OK / 返回回到错误位置；AC 一次清空。
- [ ] 普通计算和结构化应用结果进入历史、从历史恢复后的显示类型正确。

## 8. 2026-09-17 MuMu 模拟器集中验收记录

### 测试基线

- 模拟器：MuMu，Android 显示覆盖尺寸 `1260 x 2800`。
- 包名：`com.beibei.calculator`。
- 初验包：`stage6-audit-signed-apk`，artifact ID `10486064308`。
- 函数表修复复验包：`stage6-table-result-fix-signed-apk`，artifact ID `10495340801`。

### 已通过

- WorkflowAction 基础触控：四个按钮等宽、间距明确；“提高阶数 / 降低阶数”触控准确，本轮未出现相邻误触。
- 多项式二次 → 三次 → 二次：系数按幂次保留，无串位；无需再次测试。
- 联立方程二元 → 三元 → 二元：新变量列为空，RHS `b` 始终在最右侧；无需再次测试。
- 函数表修复复验：未卸载 `com.beibei.calculator`，artifact `10495340801` 通过 `adb install -r` 覆盖成功，首次安装时间保持不变；`f(x)=x`、`0–20`、步长 `1` 正常；顶部不再出现 `x,0,20,1` 和编辑光标；↓ `1–4/21` → `2–5/21`，PageDown 到 `6–9/21`，PageUp 回到 `2–5/21`；表格无错位或分页回归。
- 同包覆盖升级链：已在该 MuMu 实例上用 artifact `10495340801` 验证通过。
- 自动化补强：run `35220348788` 已验证不等式、比例、双函数的 core/Machine 交互契约并通过完整 `:core:check` 与 Android Debug 构建。
- WorkflowAction 边界自动化：commit `5bb38c502dd51988155065c4e97746052f1e42c6` / run `35226498841` 已验证统计 4 动作、矩阵/向量 6 动作顺序、最小/最大 disabled 与越界拒绝，并通过完整核心回归、Android Debug 构建和 artifact 保存。

### 尚未人工验收

- 不等式/比例/双函数的 Android 标签、布局、触控与结果视觉。
- 统计/矩阵/向量只剩操作条 Android 呈现与触控：disabled 视觉、6 按钮准确命中和相邻误触；core 边界逻辑无需重复人工验证。
- Ans、历史、复制粘贴、自然显示、语义光标与选区、DEL/AC、长按重复、SCI/ENG、长数字回归。
- 真机触感、快速滑动、多指行为；模拟器结论不能替代真机结论。

### 当前结论

PR #9 **仍不能标记为最终验收通过，也不应合并**。函数表修复与同包覆盖升级已通过复验；多项式、联立、函数表单函数无需再次测试；不等式、比例、双函数以及统计/矩阵/向量的核心边界逻辑已有永久 CI 门禁，后续人工聚焦 Android 呈现、触控和真机交互即可。

## 9. 最终验收结论记录

完成剩余测试后补：

- 设备型号 / Android 版本：
- 安装方式：覆盖安装 / 其他
- 通过项：
- 失败项：
- 失败项截图或录屏：
- 是否允许 PR #9 进入合并前收尾：是 / 否

未完成以上集中验收前，`stage6/integration` 保持唯一继续点，PR #9 保持 Draft。
